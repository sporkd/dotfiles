#! /usr/bin/env zsh

# ruby
export GEM_HOME="$(ruby -r rubygems -e 'puts Gem.user_dir')"
export BUNDLE_PATH="$GEM_HOME"
[[ -z "$GEM_HOME" ]] && unset GEM_HOME
[[ -z "$BUNDLE_PATH" ]] && unset BUNDLE_PATH
[[ -n "$GEM_HOME" ]] && path[1,0]="$GEM_HOME/bin"

# path=(${path:#})
